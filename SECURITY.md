# SBOMForge

## Security

This is a local-only tool. No network calls, no external API integrations.

- All input paths are validated
- JSON parsing errors are handled safely
- No file path traversal vulnerabilities
